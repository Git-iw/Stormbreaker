chess_engine/
    ├── engine.py        # Main entry point for the engine
    ├── board.py(optional if GUI is also needed, will delete the file later) # Board representation and drawing
    ├── bitboard.py      # Board representation in Bits using Bitboard method
    ├── move_generation.py(not optional, because after or before I implement the search and eval, I need a file so that I make moves and print it and same for the machine)  # Move generation logic
    ├── evaluation.py    # Evaluation function
    ├── search.py        # Search algorithms (e.g., Minimax, Alpha-Beta Pruning)
    ├── utils.py         # Utility functions
    ├── tests/           # Unit tests for different modules
